cmake_minimum_required(VERSION 3.22)
project(SDL3HelloTriangle VERSION 1.0.0 LANGUAGES C CXX)

if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
    include(CMakeLists.ios.cmake)
elseif(APPLE)
    include(CMakeLists.macos.cmake)
elseif(WIN32)
    include(CMakeLists.windows.cmake)
elseif(UNIX)
    include(CMakeLists.linux.cmake)
endif()